<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro - OBSF</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav>
        <ul>
            <li><a href="/">Início</a></li>
            <li><a href="/sobre">Sobre Nós</a></li>
            <li><a href="/projetos">Projetos</a></li>
            <li><a href="/como-ajudar">Como Ajudar</a></li>
            <li><a href="/contato">Contato</a></li>
            <li><a href="/login">Login</a></li>
        </ul>
    </nav>

    <div class="container">
        <div class="auth-container">
            <div class="auth-header">
                <h1>Criar Conta</h1>
                <p>Cadastre-se para fazer parte da nossa rede</p>
            </div>

            <% if (typeof erros !== 'undefined' && erros.length > 0) { %>
                <div class="notificacao erro">
                    <% erros.forEach(function(erro){ %>
                        <p><%= erro.msg %></p>
                    <% }) %>
                </div>
            <% } %>

            <% if (typeof sucesso !== 'undefined') { %>
                <div class="notificacao sucesso">
                    <p><%= sucesso %></p>
                </div>
            <% } %>

            <form class="auth-form" action="/cadastro" method="POST">
                <div class="form-group">
                    <label for="nome">Nome completo:</label>
                    <input type="text" id="nome" name="nome" required placeholder="Digite seu nome completo" value="<%= typeof nome !== 'undefined' ? nome : '' %>">
                </div>
                
                <div class="form-group">
                    <label for="email">E-mail:</label>
                    <input type="email" id="email" name="email" required placeholder="Digite seu e-mail" value="<%= typeof email !== 'undefined' ? email : '' %>">
                </div>
                
                <div class="form-group">
                    <label for="telefone">Telefone:</label>
                    <input type="tel" id="telefone" name="telefone" placeholder="Digite seu telefone" value="<%= typeof telefone !== 'undefined' ? telefone : '' %>">
                </div>
                
                <div class="form-group">
                    <label for="senha">Senha:</label>
                    <input type="password" id="senha" name="senha" required placeholder="Digite sua senha">
                </div>
                
                <div class="form-group">
                    <label for="confirmar-senha">Confirmar senha:</label>
                    <input type="password" id="confirmar-senha" name="confirmar-senha" required placeholder="Confirme sua senha">
                </div>
                
                <div class="form-group checkbox-group">
                    <input type="checkbox" id="termos" name="termos" required>
                    <label for="termos">Aceito os termos de uso e política de privacidade</label>
                </div>

                <button type="submit" class="btn-primary">Cadastrar</button>
            </form>

            <div class="auth-links">
                <p><a href="/login">Já tem conta? Faça login</a></p>
            </div>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 OBSF - Organização Brasileira Sem Fome</p>
        <p>Juntos contra a fome no Brasil</p>
    </footer>
</body>
</html>
